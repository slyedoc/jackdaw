[package]
name = "shape_game"
version = "0.1.0"
edition = "2024"
publish = false

# Standalone: detach from the jackdaw workspace this directory sits inside.
[workspace]

[dependencies]
bevy = { git = "https://github.com/bevyengine/bevy", branch = "main", default-features = false, features = ["reflect_auto_register"] }
