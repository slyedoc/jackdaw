[package]
name = "jackdaw_prefab"
version.workspace = true
edition = "2024"
description = "Prefab instancing for jackdaw scenes: the IsA vocabulary and its resolver"
license = "MIT OR Apache-2.0"
repository = "https://github.com/jbuehler23/jackdaw"

[dependencies]
bevy = { git = "https://github.com/bevyengine/bevy", branch = "main", default-features = false, features = ["std"] }
jackdaw_bsn = { version = "0.19.0", path = "../jackdaw_bsn" }
pathdiff.workspace = true
path-slash.workspace = true

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
